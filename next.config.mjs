/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  images: {
    domains: ["cdn.dummyjson.com"], // এখানে external image domain allow করো
  },
};

export default nextConfig;
